<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Keeps the track recording and the location broadcast running while the app is in the
         background or the screen is off. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- The offline-map download runs as a WorkManager foreground job. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Lets the Manual tab ask the system to exclude the app from battery optimisation, so
         long recordings survive aggressive OEM power management. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Only used to scan another instance's sharing QR code. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".TrackRecorderApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/application_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GPSTrack">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/application_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Opening a sharing link (the same payload the QR code carries) adds that instance
                 as a followed peer. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="gpstrack" android:host="peer" />
            </intent-filter>
        </activity>

        <!-- Writes GPS fixes into the Room database for the active track. -->
        <service
            android:name=".record.LocationRecordingService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Publishes this instance's position to the sharing server and pulls back the
             positions of the peers the reader is watching. -->
        <service
            android:name=".share.LocationShareService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- WorkManager's foreground service, tagged for the offline-map download. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- Quick Settings tile: start/stop track recording from the shade. -->
        <service
            android:name=".tiles.RecordTileService"
            android:exported="true"
            android:icon="@android:drawable/ic_menu_mylocation"
            android:label="@string/tile_record_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Quick Settings tile: toggle the location broadcast from the shade. -->
        <service
            android:name=".tiles.BroadcastTileService"
            android:exported="true"
            android:icon="@android:drawable/ic_menu_share"
            android:label="@string/tile_broadcast_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>
</manifest>
