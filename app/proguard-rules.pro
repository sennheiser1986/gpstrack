# Release builds keep minification off (see app/build.gradle.kts), so no custom rules are needed
# yet. Add keeps here if isMinifyEnabled is ever turned on, especially for Room-generated code
# and the ZXing embedded capture activity.
